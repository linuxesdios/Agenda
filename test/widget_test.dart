import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App se inicia correctamente', (WidgetTester tester) async {
    // Los tests se actualizarán cuando la app esté estable.
    expect(1 + 1, equals(2));
  });
}
