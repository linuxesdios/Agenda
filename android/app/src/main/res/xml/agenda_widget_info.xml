<?xml version="1.0" encoding="utf-8"?>
<!-- Configuración del widget: tamaño, intervalo de refresco, etc. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="180dp"
    android:minHeight="250dp"
    android:targetCellWidth="3"
    android:targetCellHeight="4"
    android:updatePeriodMillis="1800000"
    android:initialLayout="@layout/agenda_widget"
    android:previewImage="@mipmap/ic_launcher"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen">
    <!--
        minWidth/minHeight: tamaño mínimo en la pantalla
        targetCellWidth/Height: celdas ocupadas (Android 12+)
        updatePeriodMillis: 1800000 = 30 min (mínimo permitido por Android)
        Para refrescos más frecuentes hay que usar HomeWidget.updateWidget() desde Flutter
    -->
</appwidget-provider>
